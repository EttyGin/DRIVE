apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nginx-litellm-proxy.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "nginx-litellm-proxy.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "nginx-litellm-proxy.name" . }}
    spec:
      containers:
        - name: nginx
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/conf.d
      volumes:
        - name: nginx-config
          configMap:
            name: {{ include "nginx-litellm-proxy.fullname" . }}-config
